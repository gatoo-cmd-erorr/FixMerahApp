{
  "version": 2,
  "buildCommand": "npm install",
  "outputDirectory": null,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
